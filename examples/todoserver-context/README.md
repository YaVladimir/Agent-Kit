# Контекст пилота TodoServer

Пример контекста только для чтения для проекта:

`/path/to/TodoServer`

Пример хранится в kit, чтобы обсуждать пилот без записи в тестовый проект. Когда
решим применить его к проекту, эти файлы можно скопировать в репозиторий как
`.context/*`.
