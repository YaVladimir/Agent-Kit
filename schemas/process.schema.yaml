type: object
required:
  - id
  - title
properties:
  id:
    type: string
  title:
    type: string
  description:
    type: string
  entrypoints:
    type: array
    items:
      type: string
  services:
    type: array
    items:
      type: string
  rules:
    type: array
    items:
      type: string
  tests:
    type: array
    items:
      type: string
  steps:
    type: array
    items:
      type: object

